{
  "private": true,
  "name": "cyren",
  "scripts": {
    "build": "pnpm -r run build",
    "dev:server": "pnpm --filter @cyren/server dev",
    "dev:web": "pnpm --filter @cyren/web dev",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.5",
    "tsup": "^8",
    "vitest": "^2",
    "vite": "^6",
    "tsx": "^4"
  }
}
